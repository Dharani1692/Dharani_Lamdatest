#Introduction Project to integrate Playwright typescript with Lambdatest service

#Installation process

Run the below commands to install playwright

command: npx playwright install

Install playwright dependencies, run the below command

command: npx playwright install-deps

Run the below commands to add Lambda test username and Access key

For Mac: export LT_USERNAME="YOUR_LAMBDATEST_USERNAME" export LT_ACCESS_KEY="YOUR_LAMBDATEST_ACCESS_KEY"

For Windows: set LT_USERNAME="YOUR_LAMBDATEST_USERNAME" set LT_ACCESS_KEY="YOUR_LAMBDATEST_ACCESS_KEY"

To Run playwright tests, use the below command command: npx playwright test

